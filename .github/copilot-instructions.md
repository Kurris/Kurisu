# Kurisu 项目规范

.NET 8 WebAPI 后端框架。中文注释，英文 API 名称（类名、方法名、变量名）。**所有思考与输出均使用中文。**

所有代码、接口、配置、文档等**必须包含中文注释**，注释内容使用半角标点符号（如 , . : ; () [] 等）。

## 构建

```bash
dotnet build
```

## 测试

```bash
dotnet test
```